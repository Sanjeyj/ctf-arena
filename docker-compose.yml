version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Docker
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=ctf_super_secret_2024
