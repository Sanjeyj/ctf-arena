services:
  - type: web
    name: ctf-arena
    runtime: python
    buildCommand: pip install -r requirements.txt && python make_stego.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
