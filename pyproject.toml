[project]
name = "app"
version = "0.1.0"
dependencies = [ "flask>=3.0.0", "pillow>=10.0.0", "gunicorn>=21.2.0", "python-dotenv>=1.0.0", "pytest>=7.4.0", "flask-sqlalchemy>=3.1.0", "flask-migrate>=4.0.0", "psycopg2-binary>=2.9.9", "flask-login>=0.6.3", "bcrypt>=4.1.0", "flask-wtf>=1.2.1", "flask-limiter>=3.5.0", "docker>=7.0.0", "pyyaml>=6.0.1" ]
requires-python = "~=3.12.0"
